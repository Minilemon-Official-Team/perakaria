import type { PublicSitePayload } from "@perakaria/content-schema";
import { LayoutGridDebug } from "./LayoutGridDebug";

export function ContactSection({ content, debugGrid }: { content: PublicSitePayload; debugGrid?: boolean }) {
  const images = content.contact.galleryImages.slice(0, 9);
  const whatsapp = `https://wa.me/${content.settings.whatsapp.replace(/\D/g, "")}`;
  return <section id="contact" className="section contact" style={{ "--section-bg": content.theme.palette.backgroundPrimary, "--section-title": content.theme.palette.titlePrimary, "--section-text": content.theme.palette.textPrimary } as React.CSSProperties}><div className="section-inner contact-inner">{debugGrid && <LayoutGridDebug columns={2} />}<div className="contact-visual"><div className="bento-grid" aria-label="Perakaria studio visuals"><div className="bento-left bento-composite">{images.slice(0, 3).map((img, i) => <figure className={`bento-tile bento-left-tile-${i + 1}`} key={img.title}><img src={img.imageUrl} alt={img.imageAlt} loading="lazy" /></figure>)}</div><div className="bento-right"><div className="bento-pair bento-top">{images.slice(3, 5).map((img) => <figure className="bento-tile" key={img.title}><img src={img.imageUrl} alt={img.imageAlt} loading="lazy" /></figure>)}</div><div className="bento-middle bento-composite"><figure className="bento-tile bento-middle-tile-1"><img src={images[5]?.imageUrl} alt={images[5]?.imageAlt || "Perakaria visual"} loading="lazy" /></figure><figure className="bento-tile bento-middle-tile-2"><img src={images[6]?.imageUrl} alt={images[6]?.imageAlt || "Perakaria visual"} loading="lazy" /></figure></div><div className="bento-pair bento-bottom">{images.slice(7, 9).map((img) => <figure className="bento-tile" key={`bottom-${img.title}`}><img src={img.imageUrl} alt={img.imageAlt} loading="lazy" /></figure>)}</div></div></div></div><div className="contact-panel"><h2 className="contact-heading-1">LET'S BUILD</h2><h2 className="contact-heading-2">SOMETHING</h2><p className="contact-address">{content.settings.location}</p><div className="contact-buttons"><a className="contact-button" href={`mailto:${content.settings.email}`}>{content.contact.emailLabel}</a><a className="contact-button" href={whatsapp} target="_blank" rel="noopener noreferrer">{content.contact.whatsappLabel}</a></div></div></div></section>;
}